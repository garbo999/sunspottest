<h1>Search results for "<%= params[:search] %>" (page <%= @current_page %> of <%= @search.results.total_pages %>)</h1>

<h4>(<%= @search.total %> <%= @search.total == 1 ? "result" : "total results" %> found)</h4>

<p> <%= link_to "Previous page", root_path(search: params[:search], page: @search.results.previous_page) %> 
    <%= link_to "Next page", root_path(search: params[:search], page: @search.results.next_page) %> </p>

